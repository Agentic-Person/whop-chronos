# ============================================================================
# Whop Integration (Native Authentication)
# ============================================================================
# For embedded apps, Whop handles auth via JWT tokens in the x-whop-user-token header.
# OAuth is DEPRECATED - use native auth for embedded apps.
# See: https://docs.whop.com/developer/guides/authentication

WHOP_API_KEY=your_whop_api_key # REQUIRED - Get from https://dev.whop.com
WHOP_APP_ID=app_xxxxxxxxxxxxxx # REQUIRED - Get from Whop Developer Dashboard (starts with "app_")
WHOP_WEBHOOK_SECRET=your_whop_webhook_secret # REQUIRED for webhooks

# Development Authentication Bypass
# Set to "true" to bypass Whop auth in local development (uses test IDs)
DEV_BYPASS_AUTH=false
NEXT_PUBLIC_DEV_BYPASS_AUTH=false

# ============================================================================
# DEPRECATED OAuth Configuration (Only for standalone apps)
# ============================================================================
# These are NOT needed for embedded apps using native authentication.
# Only use if you're building a standalone app with OAuth flow.
# WHOP_CLIENT_ID=your_whop_client_id
# WHOP_CLIENT_SECRET=your_whop_client_secret
# WHOP_TOKEN_ENCRYPTION_KEY=your_64_character_hex_encryption_key
# WHOP_OAUTH_REDIRECT_URI=${NEXT_PUBLIC_APP_URL}/api/whop/auth/callback

# Whop MCP (Model Context Protocol) - For AI assistants during development only
# Server installed at: ~/.mcp/servers/whop/
# See whop.mcp.json for configuration
# Documentation: ~/.mcp/servers/whop/README.md

# Plan Checkout URLs (Optional - Get from Whop dashboard)
NEXT_PUBLIC_WHOP_BASIC_CHECKOUT_URL=https://whop.com/checkout/plan_basic
NEXT_PUBLIC_WHOP_PRO_CHECKOUT_URL=https://whop.com/checkout/plan_pro
NEXT_PUBLIC_WHOP_ENTERPRISE_CHECKOUT_URL=https://whop.com/checkout/plan_enterprise

# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Supabase MCP Server (for AI assistant database access during development)
# Get from: https://supabase.com/dashboard/account/tokens
# 1. Go to your Supabase account settings
# 2. Click "Access Tokens" in the left sidebar
# 3. Click "Generate New Token"
# 4. Give it a name like "Claude Code MCP"
# 5. Copy the token (starts with "sbp_")
SUPABASE_ACCESS_TOKEN=your_supabase_personal_access_token

# AI Services
ANTHROPIC_API_KEY=your_anthropic_api_key
# Claude Model Selection - Configures which Claude model to use
# See lib/config/ai-models.ts for full details on each model
#
# Available Options:
#   claude-3-5-haiku-20241022   (DEFAULT) - Fastest, most cost-effective (~92% cheaper than Sonnet)
#                                 Best for: High-volume chat, quick responses, RAG queries
#                                 Cost: $0.25/$1.25 per million tokens (input/output)
#
#   claude-3-5-sonnet-20241022  - Balanced intelligence, speed, and cost
#                                 Best for: Complex reasoning, detailed analysis
#                                 Cost: $3/$15 per million tokens (input/output)
#
#   claude-3-opus-20240229      - Most intelligent, best for complex tasks
#                                 Best for: Deep reasoning, difficult problems
#                                 Cost: $15/$75 per million tokens (input/output)
#
# To switch models: Just change this variable and restart the app. No code changes needed!
ANTHROPIC_MODEL=claude-3-5-haiku-20241022
OPENAI_API_KEY=your_openai_api_key

# YouTube Integration (for video import feature)
# Get API key from: https://console.cloud.google.com/
# 1. Create/select a project
# 2. Enable "YouTube Data API v3"
# 3. Go to Credentials → Create Credentials → API Key
# 4. (Optional but recommended) Restrict the key:
#    - Application restrictions: HTTP referrers (add your domain)
#    - API restrictions: YouTube Data API v3 only
YOUTUBE_API_KEY=your_youtube_api_key_here

# Storage
# Using Supabase Storage - no additional config needed
# Videos are stored in Supabase Storage buckets

# Discord Bot (ENTERPRISE tier feature)
DISCORD_BOT_TOKEN=your_discord_bot_token
DISCORD_CLIENT_ID=your_discord_client_id
DISCORD_CLIENT_SECRET=your_discord_client_secret
DISCORD_GUILD_ID=your_discord_server_id
ENABLE_DISCORD_BOT=false # Set to true to enable bot in development

# Monitoring & Error Tracking
# Get from: https://sentry.io/
SENTRY_DSN=your_sentry_dsn
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn # Same as SENTRY_DSN (for client-side error tracking)
SENTRY_AUTH_TOKEN=your_sentry_auth_token
SENTRY_ORG=your_sentry_org
SENTRY_PROJECT=chronos
NEXT_PUBLIC_POSTHOG_KEY=your_posthog_key

# Cache (Vercel KV / Upstash Redis)
# Get these from: https://vercel.com/dashboard/stores (Vercel KV)
# Or: https://upstash.com/ (Upstash Redis)
VERCEL_KV_URL=
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Job Queue (Inngest)
INNGEST_EVENT_KEY=evt_xxxxx
INNGEST_SIGNING_KEY=sig_xxxxx

# Email (Resend)
RESEND_API_KEY=re_xxxxx
RESEND_FROM_EMAIL=noreply@mentora.com

# App Config
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development
LOG_LEVEL=debug
